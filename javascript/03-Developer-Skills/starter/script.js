// Remember, we're gonna use strict mode in all scripts now!
'use strict';

/*const printForcast = function(arr){
    const degreeNew = [];
    for(let i = 0; i < arr.length; i++){
        let degree = `${arr[i]}°C in ${++i} days...`;
        i--;
        degreeNew.push(degree);
    }
    console.log("..." + degreeNew);    
}
 
const teatData = [12, 5, -5, 8, 0, 4];
printForcast(teatData);*/

/*const data1 = [12, 5, -5, 8, 0, 4];
 const printForcast = function(arr){
     let str = '... ';
     for(let i = 0; i < arr.length; i++){
     str = str+`${arr[i]}°C in ${i+1} days ... `;
     }
     console.log(str);
 }
 printForcast(data1);*/